<!--
  api-foundry-embeddings-policy.xml  -  API scope (foundry-embeddings-api).
  Routes to pool-foundry-embeddings. Embeddings responses are non-streaming, so
  no stream_options handling is required; token accounting uses actual usage.
  See gpt5-nano policy for reference links.
-->
<policies>
  <inbound>
    <base />
    <!-- (1) Per-key token governance: shared lifetime/burst budget via counter-key.
         Header attributes are valid here (API scope) but not at global/product scope. -->
    <llm-token-limit
      counter-key="@(context.Subscription.Id)"
      tokens-per-minute="{{TPM_GUARDRAIL}}"
      token-quota="{{TOKEN_QUOTA}}"
      token-quota-period="{{TOKEN_QUOTA_PERIOD}}"
      estimate-prompt-tokens="false"
      tokens-consumed-header-name="x-tokens-consumed"
      remaining-tokens-header-name="x-tokens-remaining-minute"
      remaining-quota-tokens-header-name="x-tokens-remaining-quota"
      retry-after-header-name="x-retry-after" />
    <set-backend-service backend-id="pool-foundry-embeddings" />
    <authentication-managed-identity resource="https://cognitiveservices.azure.com" output-token-variable-name="msi-access-token" ignore-error="false" />
    <set-header name="Authorization" exists-action="override">
      <value>@("Bearer " + (string)context.Variables["msi-access-token"])</value>
    </set-header>
    <set-header name="api-key" exists-action="delete" />
  </inbound>
  <backend>
    <base />
  </backend>
  <outbound>
    <base />
  </outbound>
  <on-error>
    <base />
  </on-error>
</policies>
